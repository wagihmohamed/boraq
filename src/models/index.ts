export interface Employee {
  id: number;
  name: string;
  role: string;
  branch: string;
  location: string;
  phone: string;
}

export interface Repository {
  id: number;
  name: string;
  branch: string;
}
